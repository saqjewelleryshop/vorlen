import { site } from '@/lib/site';
export function GET(){const body=`# Vorlen\n\n> Independent digital studio in Manchester, UK.\n\nVorlen designs and engineers websites, web applications, ecommerce experiences, AI automation and search-led growth systems.\n\n## Key pages\n- ${site.url}/services — services overview\n- ${site.url}/work — selected work\n- ${site.url}/about — studio and approach\n- ${site.url}/insights — practical articles\n- ${site.url}/contact — project enquiries\n\n## Contact\n${site.email}\n`;return new Response(body,{headers:{'Content-Type':'text/plain; charset=utf-8','Cache-Control':'public, max-age=3600'}})}
